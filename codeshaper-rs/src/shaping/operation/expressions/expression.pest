// Common Expression Grammar

expression = { SOI ~ expr ~ EOI }
expr = { "#" ~ "[" ~ name ~ "]" ~ arguments? }
arguments = { "(" ~ ( (expr | name) | "," ~ WS )*? ~ ")" }
arg = { WS ~ name ~ WS }

name = { char+ }
char = _{ ASCII_ALPHANUMERIC | "_" | "-" }

WS = _{ " " }

